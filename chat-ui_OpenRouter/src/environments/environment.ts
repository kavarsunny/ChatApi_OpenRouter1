export const environment = {
  production: true,
  // This will be your Render backend URL once deployed. 
  // Make sure to replace this with the actual URL from Render later!
  apiUrl: 'https://tatva-e-seva-api.onrender.com/api/chat'
};
